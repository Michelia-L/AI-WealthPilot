# Development dependencies — runtime set plus test tooling.
#   pip install -r requirements-dev.txt
#
# Pinned like requirements.txt (see its header note).

-r requirements.txt

# Testing & CI quality gates (coverage + dependency CVE scan)
pytest==9.1.1
pytest-cov==7.1.0
pip-audit==2.10.1

# Lint & format gates (config in pyproject.toml)
ruff==0.16.4

# Documentation site (mkdocs.yml, deployed to GitHub Pages by docs.yml)
mkdocs-material==9.7.7
jieba==0.42.1  # build-time CJK segmentation for the search plugin (verified working)
