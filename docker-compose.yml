# AI WealthPilot — local-first deployment.
#
#   docker compose up --build
#
# Web:  http://localhost:3000  (Next.js)
# API:  http://localhost:8000  (FastAPI, docs at /docs)
#
# Ports bind to 127.0.0.1 on purpose: the API has no authentication, so it
# must not be reachable from the LAN. If you ever need to expose it (VPS,
# LAN sharing), override ports in a docker-compose.override.yml — and add
# authentication first.

services:
  api:
    build:
      context: .          # repository root — the API imports the src/ package
      dockerfile: api/Dockerfile
    env_file:
      - path: .env        # optional: FRED_API_KEY / DEEPSEEK_API_KEY
        required: false
    volumes:
      - ./data:/app/data  # CME cache, profiles, reports stay on the host
    ports:
      - "127.0.0.1:8000:8000"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health', timeout=5)"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  web:
    build:
      context: ./web
    environment:
      # Server Components fetch the API over the internal compose network.
      API_ORIGIN: http://api:8000
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      api:
        condition: service_healthy
