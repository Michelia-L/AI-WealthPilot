"""
AI WealthPilot - Pages Package
AI WealthPilot - 页面包

This package contains individual Streamlit page modules.
Each page module exposes a render() function that is called
by the main app router (src/app.py).

本包包含各个 Streamlit 页面模块。
每个页面模块暴露一个 render() 函数，
由主应用路由（src/app.py）调用。
"""
