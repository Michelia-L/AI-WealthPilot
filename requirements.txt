# Runtime dependencies — FastAPI shell + quant core (this is the exact set
# the API Docker image installs). Dev extras (pytest) live in
# requirements-dev.txt.
#
# Versions are pinned to the set the test suite (880 tests) was verified
# against; bump deliberately after re-running the suite. pip-audit in CI
# guards the pinned set against newly disclosed CVEs.

# Core
numpy==2.2.6
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0

# Data Sources
yfinance==1.4.0

# Visualization (figures are generated server-side, streamed as JSON)
plotly==6.6.0

# AI / LLM (DeepSeek V4 Pro — OpenAI-compatible API)
openai==2.41.0

# Agent Orchestration — IPS Generator (LangGraph + PydanticAI)
langgraph==1.2.4
langgraph-checkpoint==4.1.1
pydantic-ai==1.107.1
pydantic==2.12.5

# Utilities
python-dotenv==1.2.2
requests==2.33.0  # 2.33.0 fixes PYSEC-2026-2275 (pip-audit finding)
markdown==3.10.3
nh3==0.3.5  # HTML sanitizer for LLM-generated report content (XSS prevention)
fpdf2==2.8.7

# API Layer
fastapi==0.136.1
uvicorn[standard]==0.46.0
sqlmodel==0.0.39  # SQLite persistence (client profiles, future user tables)
tushare==1.4.29
