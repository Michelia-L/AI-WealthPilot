# API image dependency set (slim) — everything the FastAPI shell + quant core
# needs at runtime, minus Streamlit (only imported by src/app.py + src/views/)
# and pytest. Keep in sync with requirements.txt; the full file stays the
# dev/Streamlit environment until Phase 6 retires Streamlit.

# Core
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.2.0

# Data Sources
yfinance>=0.2.28

# Visualization (figures are generated server-side, streamed as JSON)
plotly>=5.18.0

# AI / LLM (DeepSeek V4 Pro — OpenAI-compatible API)
openai>=1.0.0

# Agent Orchestration — IPS Generator (LangGraph + PydanticAI)
langgraph>=0.2.0
langgraph-checkpoint>=2.0.0
# OpenAIModel was removed in pydantic-ai 1.107+; src/agents/ips_agents.py
# still uses it (src stays untouched during the Streamlit migration).
pydantic-ai>=1.0.0,<1.107.0
pydantic>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
markdown>=3.4.0
nh3>=0.2.0  # HTML sanitizer for LLM-generated report content (XSS prevention)
fpdf2>=2.8.0

# API Layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlmodel>=0.0.22
